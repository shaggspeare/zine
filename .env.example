# Server-side only: the service role key bypasses row level security and must
# never reach the browser. `supabase status` prints both for local development.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=
